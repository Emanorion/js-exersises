colors = [];
colors.push("Red","Purple","Grey");
console.log(colors.join());


numbers = [1,2,3,4,5];
colors.pop();
colors.unshift("Black")
console.log(numbers.join());
console.log(colors.join());

console.log(`The leghth of the array colors is ${colors.length} and numbers is ${numbers.length}`);


